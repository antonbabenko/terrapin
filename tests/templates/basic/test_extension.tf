# Arguments (variables):
# type1 (String)=TypeString
# type2 (String)=TypeString
# type3 (Map)=TypeMap
# type4 (Int)=TypeInt
# type5 (List)=TypeList
# Attributes (outputs):
# attr1=input,keepers,result,result_count,seed,id
# attr2=arn,expiration_date,id,latest,name,name_prefix,path
